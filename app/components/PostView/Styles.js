define( function( require ) {
	var $            = require( 'jquery' );
	var GlobalStyles = require( 'GlobalStyles' );

	return {
		post: {
			marginTop: 30,
			borderBottom: '1px solid lightgrey',
			padding: 15
		},
		author: {
			fontSize: '2rem',
			marginBottom: 10
		}
	};
});