define( function( require ) {
	var $            = require( 'jquery' );
	var GlobalStyles = require( 'GlobalStyles' ); 

	return {
		button: {

		},
		inputGroup: {

		},
		container: GlobalStyles.container,
		title: {
			fontSize: '2.8rem',
			marginBottom: 30
		},
	};
});